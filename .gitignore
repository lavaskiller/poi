# dependencies
node_modules
# build
dist
dist-ssr
# TS/Vite build artifacts (generated)
*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.d.ts.map
# logs
*.log
npm-debug.log*
# editor / os
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# secrets / keys — never commit
.ssh
*.pem
*_key
*_key.pub

# datasets / run snapshots — real user data, shared privately (GDrive), never online
poi-data
poi-data-seed
# defense-in-depth: if a seed/ingest ever materializes data at the repo root
# (legacy layout), keep it out of git. dashboard_config.json stays tracked (it
# is the boot-time config template) and is never overwritten by the seed.
/eval_set_reconciled.csv
/generated/
.grok
.worktrees
linkedspaces_visual_solvability_audit.md
docs/reports
