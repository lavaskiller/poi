# dependencies
node_modules
# build
dist
dist-ssr
# logs
*.log
npm-debug.log*
# editor / os
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# secrets / keys — never commit
.ssh
*.pem
*_key
*_key.pub

# local backend data carried over (not part of the frontend repo)
poi-data
.grok
.worktrees
linkedspaces_visual_solvability_audit.md
docs/reports
