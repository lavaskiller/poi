# Cleanup 2026-07-08

Removed root-level throwaway artifacts from early POI experiments.

## Deleted categories

- Empty or transient `*_err.txt` log files.
- Stale Swift build artifact: `ai_poi_run` binary. Source remains in `ai_poi_run.swift`.
- Obsolete early AI experiment TSVs:
  - `ai_cc_v*.tsv`: coords+caption runs that mostly failed with unsupported language/locale.
  - `ai_results_*.tsv`: early pre-variant arm outputs superseded by `ai_ocr_v2.tsv` / `ai_ocr_v3.tsv`.
  - `ai_input.tsv`: old input for the superseded early arm outputs.

## Kept intentionally

- Main data: `eval_set_reconciled.csv`, `eval_set_combined.csv`, `eval_set.csv`.
- Active dashboard config: `dashboard_config.json`.
- Active AI/OCR runs: `ai_input_ocr.tsv`, `ai_ocr_v2.tsv`, `ai_ocr_v3.tsv`.
- MapKit/OCR merge inputs: `ls_nearby_results.tsv`, `ls_ocr_text.tsv`, `ocr_text.tsv`, probe inputs.
- Backup CSVs for the main reconciled dataset.
- Source scripts and Swift probes.
