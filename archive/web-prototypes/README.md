# Archived web prototypes

These standalone HTML files are earlier dashboard and documentation prototypes. They are retained for historical reference only.

They are **not** active application pages and are not listed as supported server routes. Use [`../../mvp-eval-ui.html`](../../mvp-eval-ui.html) (or the server root URL) for the current interface.
