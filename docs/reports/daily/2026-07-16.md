# Daily report — 2026-07-16

## Documentation and evaluation review

- Audited the current evaluation input path and found that the retained MapKit
  candidate corpus is not a uniformly complete Top-5 corpus. Historical
  reranker scores must therefore not be described as a general Top-5 result.
- Reviewed post-ingest parallel enrichment: EXIF is the required precursor for
  coordinate-dependent work; OCR, nearby MapKit retrieval, and GT
  classification can overlap, but MapKit concurrency should be measured and
  bounded rather than assumed to be faster.
- Confirmed that reverse geocoding is useful for country/provider routing and
  geographic context, but cannot create POI ground truth. No CLGeocoder worker
  is implemented at this point, so the pipeline explicitly skips geocoding.
- Reorganized documentation: active reports are now a continuous daily record;
  duplicate, superseded, and snapshot-specific reports were deleted from the
  working tree. Git history is the recovery mechanism.
- Restored the local case-explorer HTML after it was incorrectly deleted as a
  legacy report despite not being Git-tracked. Removed its redundant per-run
  correct/wrong badges: each prediction box already presents the run name,
  prediction, and correctness icon. The distinct GT-candidate-coverage badge
  remains because it communicates input coverage rather than a prediction.

## Evidence

These are current working-tree documentation and code-review findings, not a
new committed product release at the time of writing. The deleted historical
analyses remain recoverable from Git history.

## Follow-up

- Add stage/provider timing, retry, and error telemetry before tuning
  enrichment parallelism.
- If reverse geocoding is implemented, use conservative fill-empty behavior,
  ISO country codes, provenance, cache/rate-limit handling, and `unknown`/
  holdout on unresolved rows.
- Recollect complete candidate responses before making Top-K reranker claims.
